import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#01204E',
    },
    secondary: {
      main: '#028391',
    },
    background: {
      default: '#F6DCAC',
    },
    warning: {
      main: '#FEAE6F',
    },
  },
});

export default theme;
